/** Default paid managers (seats) per Creatix subscription checkout. */
export const DEFAULT_BILLING_SEATS = 1

/** Max seats per subscription (Stripe line quantity). */
export const MAX_BILLING_SEATS = 20
