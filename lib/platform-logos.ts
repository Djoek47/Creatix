/**
 * Primary OnlyFans + Fansly lockups (SVG wordmarks in `/public`).
 * Use these instead of the legacy “OF” circle or ad-hoc paths so the app matches the marketing home hero.
 */
export const ONLYFANS_LOGO_SRC = '/onlyfans-logo.svg' as const
export const FANSLY_LOGO_SRC = '/fansly-logo.svg' as const
