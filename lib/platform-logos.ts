/**
 * OnlyFans + Fansly assets in `/public` — your real logo files (`onlyfans-logo.png`, `fansly-logo.png`).
 * The app must not generate or replace these; update the PNGs in `public/` when you change artwork.
 */
export const ONLYFANS_LOGO_SRC = '/onlyfans-logo.png' as const
export const FANSLY_LOGO_SRC = '/fansly-logo.png' as const
